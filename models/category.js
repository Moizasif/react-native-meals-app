class Category {
    //store data with properties with the help of this keyword
    constructor(id, title, color) {
         this.id = id;
         this.title = title;
         this.color = color;
    }
}

export default Category;